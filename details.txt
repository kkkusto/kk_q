filtered_data = data[data['JOB'] == selected_job]

# Convert Date to just date (no time) if it includes time
filtered_data['Date'] = filtered_data['Date'].dt.date

# Plotting
plt.figure(figsize=(10, 6))
sns.boxplot(x='Date', y='COUNT1', data=filtered_data)
plt.xticks(rotation=45)  # Rotate date labels for better readability
plt.title(f'Count Distribution per Date for {selected_job}')
plt.xlabel('Date')
plt.ylabel('Count')

# Show the plot in the Streamlit app
st.pyplot(plt)
